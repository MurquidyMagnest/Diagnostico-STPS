@model IEnumerable<API_STPS.Models.Incisos_normas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id_noms)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.inciso_noms)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.comprobacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.criterio_acepton)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.observacion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id_noms)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.inciso_noms)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.comprobacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.criterio_acepton)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.observacion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
